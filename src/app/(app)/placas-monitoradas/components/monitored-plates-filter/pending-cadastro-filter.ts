/**
 * TEMPORARY — TODO(pending-cadastro)
 *
 * Remove this module and every `TODO(pending-cadastro)` usage once no
 * monitored plates remain without a requester link.
 *
 * Find later: grep TODO(pending-cadastro)
 */
export const PENDING_CADASTRO_FILTER_ID = 'none'
export const PENDING_CADASTRO_FILTER_LABEL = 'NENHUM - PENDENTE CADASTRO'
